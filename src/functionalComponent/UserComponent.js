function UserComponent() {
    return (
        <div className="App">
            <h1>What is the Functional Component?</h1>
            <pre>Functional Component only contain a render method and don't have their own state. 
                They are simply JavaScript functions that may or may not receive data as parameters</pre>
        </div>
    );
}
export default UserComponent;